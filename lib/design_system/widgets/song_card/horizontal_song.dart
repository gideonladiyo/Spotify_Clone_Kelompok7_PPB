import 'package:flutter/material.dart';

class HorizontalSong extends StatelessWidget {
  const HorizontalSong({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}